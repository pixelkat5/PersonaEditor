<UserControl x:Class="PersonaEditor.Views.Editors.HIPFontEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="500" d:DesignWidth="900">
    <Grid Margin="6">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="6"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <ListBox x:Name="GlyphList"
                 ItemsSource="{Binding Glyphs}"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ScrollViewer.CanContentScroll="True"
                 VirtualizingPanel.IsVirtualizing="True"
                 VirtualizingPanel.VirtualizationMode="Recycling">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate><VirtualizingStackPanel/></ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Height="62" Margin="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="56"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border BorderBrush="Gray" BorderThickness="1" Width="54" Height="54">
                            <Image Source="{Binding Image}" Stretch="Uniform" RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                        </Border>
                        <StackPanel Grid.Column="1" Margin="8,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Characters, Mode=OneWay}" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{Binding CodePoints, Mode=OneWay}" FontSize="11" Foreground="Gray" TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch"/>

        <Grid Grid.Column="2" Margin="12" DataContext="{Binding SelectedItem, ElementName=GlyphList}">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="3*"/>
            </Grid.RowDefinitions>
            <Border BorderBrush="Gray" BorderThickness="1" Padding="8">
                <Image Source="{Binding Image, Mode=OneWay}" Stretch="Uniform" RenderOptions.BitmapScalingMode="NearestNeighbor"/>
            </Border>
            <TextBlock Grid.Row="1" Margin="0,8" HorizontalAlignment="Center">
                <Run Text="{Binding Characters, Mode=OneWay}"/><Run Text="  "/><Run Text="{Binding CodePoints, Mode=OneWay}"/><Run Text="  Glyph "/><Run Text="{Binding Index, Mode=OneWay}"/>
            </TextBlock>
            <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1" Padding="8" Margin="6">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Map to" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBox x:Name="MappingTarget" Grid.Column="1" Margin="0,0,8,0"/>
                    <Button Grid.Column="2" Content="Apply" Click="ApplyMapping_Click" Padding="12,2"/>
                    <CheckBox x:Name="ClearSelectedMappings" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Content="Clear old mappings" IsChecked="True" Margin="0,6,0,0"/>
                </Grid>
            </Border>
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
                <StackPanel Margin="6">
                    <TextBlock Text="Layout" FontWeight="Bold" Margin="0,0,0,6"/>
                    <TextBlock Text="Left bearing"/>
                    <TextBox Text="{Binding Left, UpdateSourceTrigger=LostFocus}" TextChanged="FieldChanged"/>
                    <TextBlock Text="Bitmap width" Margin="0,6,0,0"/>
                    <TextBox Text="{Binding Width, UpdateSourceTrigger=LostFocus}" TextChanged="FieldChanged"/>
                    <TextBlock Text="Advance" Margin="0,6,0,0"/>
                    <TextBox Text="{Binding Advance, UpdateSourceTrigger=LostFocus}" TextChanged="FieldChanged"/>
                    <TextBlock Text="Top offset" Margin="0,6,0,0"/>
                    <TextBox Text="{Binding Top, UpdateSourceTrigger=LostFocus}" TextChanged="FieldChanged"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="6">
                    <TextBlock Text="Atlas rectangle" FontWeight="Bold" Margin="0,0,0,6"/>
                    <TextBlock Text="X"/>
                    <TextBox Text="{Binding X, UpdateSourceTrigger=LostFocus}" TextChanged="FieldChanged"/>
                    <TextBlock Text="Y" Margin="0,6,0,0"/>
                    <TextBox Text="{Binding Y, UpdateSourceTrigger=LostFocus}" TextChanged="FieldChanged"/>
                    <TextBlock Text="Right" Margin="0,6,0,0"/>
                    <TextBox Text="{Binding Right, UpdateSourceTrigger=LostFocus}" TextChanged="FieldChanged"/>
                    <TextBlock Text="Bottom" Margin="0,6,0,0"/>
                    <TextBox Text="{Binding Bottom, UpdateSourceTrigger=LostFocus}" TextChanged="FieldChanged"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
